@using Sportify.ViewModels;
@model VerifyEmail;

@{
    ViewData["Title"] = "E-posta Doğrula";
    Layout = "~/Views/Shared/_AccountLayout.cshtml";
}

<style>
    /* Genel stiller, Login.cshtml'deki ile aynıdır */
    :root {
        --primary-green: #10b981;
        --dark-navy: #1f2937;
        --bg-gray: #f3f4f6;
    }

    .account-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background-color: var(--bg-gray);
        padding: 20px;
    }

    .account-box {
        background: white;
        padding: 40px;
        border-radius: 20px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
        width: 100%;
        max-width: 420px;
        border-top: 5px solid #f59e0b; /* Sarımsı/Turuncu vurgu */
    }

        .account-box h2 {
            font-weight: 800;
            color: var(--dark-navy);
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
        }

    .form-label {
        font-weight: 600;
        color: #4b5563;
        margin-bottom: 0.3rem;
        font-size: 0.9rem;
    }

    .form-control {
        border: 2px solid #e5e7eb;
        border-radius: 12px;
        padding: 12px 15px;
        transition: all 0.3s;
        width: 100%;
        box-shadow: none;
    }

        .form-control:focus {
            border-color: #f59e0b;
            box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.2);
            outline: none;
        }

    .btn-verify {
        background-color: #f59e0b;
        color: white;
        padding: 15px 0;
        border-radius: 12px;
        font-weight: 700;
        border: none;
        font-size: 1rem;
        cursor: pointer;
        transition: background-color 0.2s;
        margin-top: 1rem;
    }

        .btn-verify:hover {
            background-color: #d97706;
        }

    .back-link {
        font-weight: 500;
        color: #6b7280 !important;
    }
</style>

<div class="account-container">
    <div class="account-box">
        <h2 class="text-center mb-4"><i class="fas fa-envelope" style="color: #f59e0b; margin-right: 10px;"></i> E-posta Doğrula</h2>
        <p class="text-center text-muted mb-4" style="font-size:0.9rem;">Şifre yenileme veya hesap doğrulama için e-posta adresini gir.</p>

        <form asp-action="VerifyEmail" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="Email" class="form-label">E-posta Adresi</label>
                <input asp-for="Email" class="form-control" placeholder="Doğrulanacak e-posta" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <input type="submit" value="Doğrulama Kodunu Gönder" class="btn btn-verify w-100 p-2" />

            <div class="text-center mt-3">
                <a asp-controller="Account" asp-action="Login" class="text-decoration-none mt-3 back-link">Giriş Sayfasına Dön</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial.cshtml");
    }
}